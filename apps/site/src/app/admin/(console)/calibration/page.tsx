import { OpsCalibration } from "@/components/admin/ops-calibration";

export const metadata = {
  title: "Calibration — MotiveFX Ops",
  robots: { index: false, follow: false },
};

export default function Page() {
  return <OpsCalibration />;
}
