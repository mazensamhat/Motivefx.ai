import { OpsProviders } from "@/components/admin/ops-providers";

export const metadata = {
  title: "Providers — MotiveFX Ops",
  robots: { index: false, follow: false },
};

export default function OpsProvidersPage() {
  return <OpsProviders />;
}
