import { OpsSettings } from "@/components/admin/ops-settings";

export const metadata = {
  title: "Settings — MotiveFX Ops",
  robots: { index: false, follow: false },
};

export default function OpsSettingsPage() {
  return <OpsSettings />;
}
