import { OpsEvidenceQuality } from "@/components/admin/ops-evidence-quality";

export const metadata = {
  title: "Evidence Quality — MotiveFX Ops",
  robots: { index: false, follow: false },
};

export default function Page() {
  return <OpsEvidenceQuality />;
}
