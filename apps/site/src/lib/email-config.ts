/** Shared Resend account with Motive Life — no second domain required. */
export const SHARED_RESEND_DOMAIN = "mymotivelife.com";

/** Verified sender on the shared Resend account (display name: MotiveFX). */
export const DEFAULT_EMAIL_FROM = `MotiveFX <hello@${SHARED_RESEND_DOMAIN}>`;

export const DEFAULT_CONTACT_EMAIL = `hello@${SHARED_RESEND_DOMAIN}`;
