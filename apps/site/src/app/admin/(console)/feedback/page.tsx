import { OpsFeedback } from "@/components/admin/ops-feedback";

export const metadata = {
  title: "Feedback — MotiveFX Ops",
  robots: { index: false, follow: false },
};

export default function OpsFeedbackPage() {
  return <OpsFeedback />;
}
