import { OpsAiAssistant } from "@/components/admin/ops-ai-assistant";

export const metadata = {
  title: "AI Ops Assistant — MotiveFX Ops",
  robots: { index: false, follow: false },
};

export default function Page() {
  return <OpsAiAssistant />;
}
