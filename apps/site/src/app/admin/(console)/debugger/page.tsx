import { OpsIntelligenceDebugger } from "@/components/admin/ops-intelligence-debugger";

export const metadata = {
  title: "Intelligence Debugger — MotiveFX Ops",
  robots: { index: false, follow: false },
};

export default function Page() {
  return <OpsIntelligenceDebugger />;
}
