import { OpsAiOperations } from "@/components/admin/ops-ai-operations";

export const metadata = {
  title: "AI Operations — MotiveFX Ops",
  robots: { index: false, follow: false },
};

export default function Page() {
  return <OpsAiOperations />;
}
