import { OpsRevenue } from "@/components/admin/ops-revenue";

export const metadata = {
  title: "Revenue — MotiveFX Ops",
  robots: { index: false, follow: false },
};

export default function OpsRevenuePage() {
  return <OpsRevenue />;
}
