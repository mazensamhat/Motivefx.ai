import { OpsMarketDna } from "@/components/admin/ops-market-dna";

export const metadata = {
  title: "Market DNA — MotiveFX Ops",
  robots: { index: false, follow: false },
};

export default function Page() {
  return <OpsMarketDna />;
}
