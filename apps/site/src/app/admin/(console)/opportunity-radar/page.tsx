import { OpsOpportunityRadar } from "@/components/admin/ops-opportunity-radar";

export const metadata = {
  title: "Opportunity Radar — MotiveFX Ops",
  robots: { index: false, follow: false },
};

export default function Page() {
  return <OpsOpportunityRadar />;
}
