import { OpsPipelines } from "@/components/admin/ops-pipelines";

export const metadata = {
  title: "Pipelines — MotiveFX Ops",
  robots: { index: false, follow: false },
};

export default function Page() {
  return <OpsPipelines />;
}
