import { OpsProduct } from "@/components/admin/ops-product";

export const metadata = {
  title: "Product — MotiveFX Ops",
  robots: { index: false, follow: false },
};

export default function OpsProductPage() {
  return <OpsProduct />;
}
